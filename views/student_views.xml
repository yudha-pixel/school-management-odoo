<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- LIST VIEW -->
    <record id="view_school_student_list" model="ir.ui.view">
        <field name="name">school.student.list</field>
        <field name="model">school.student</field>
        <field name="arch" type="xml">
            <list string="Students" sample="1">
                <field name="name"/>
                <field name="birth_date"/>
                <field name="gender"/>
                <field name="class_id"/>
            </list>
        </field>
    </record>

    <!-- FORM VIEW -->
    <record id="view_school_student_form" model="ir.ui.view">
        <field name="name">school.student.form</field>
        <field name="model">school.student</field>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <form string="Student">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="birth_date"/>
                        <field name="gender"/>
                        <field name="class_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_school_student" model="ir.actions.act_window">
        <field name="name">Students</field>
        <field name="res_model">school.student</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- MENUS -->
    <menuitem id="menu_school_root" name="School"/>
    <menuitem id="menu_school_students" name="Students" parent="menu_school_root"/>
    <menuitem id="menu_school_student_action" name="All Students" parent="menu_school_students" action="action_school_student"/>
</odoo>
